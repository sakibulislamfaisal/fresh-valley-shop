import React from "react";

const Delivery = ({ cart , deliveryInfo,handleDeliveryInfo,checkOutProduct,handleUserEmail,clearCart}) => {
  console.log("Delivery", cart);
  return (
    <div>
      <h3>This is delivery page</h3>
    </div>
  );
};

export default Delivery;
